# ── Secretos (los reales viven en ~/.faro/.env, FUERA del repo) ──
.env
**/.env
*.pem
*.key

# ── Dependencias ──
node_modules/

# ── Perfiles temporales que crea Chrome al generar PDFs ──
tools/audit-generator/.pdf-profile_*

# ── PDFs de audits: se generan en LOCAL y SÍ se versionan (la nube solo los adjunta) ──

# ── Artefactos / transitorios (se regeneran en cada ejecución) ──
panel-interno.html
targets/envios-HOY-*.json
targets/envios-HOY-*.csv
targets/inbox-state-*.json
targets/preflight-*.json
targets/lote-*.csv

# ── Capturas, logs, basura del SO ──
*.png
*.log
desktop.ini
Thumbs.db
.DS_Store
